# Changelog

## Unreleased

- Initial Python port from `render-examples/sf-pulse-ts` ([commit a02a8513](https://github.com/render-examples/sf-pulse-ts/commit/a02a8513)).
- FastAPI + asyncpg + Render Python SDK + Vite/React diagram bundle.
- Render Blueprint (`render.yaml`) provisioning web + cron + Postgres + Redis. Workflow service is configured manually in the Dashboard.
